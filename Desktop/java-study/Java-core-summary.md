# Java Core Summary

## 1. Java 기본 구조 & 실행 과정

```java
public class Main {
    public static void main(String[] args) {
        System.out.println("Hello Java");
    }
}
```

* 모든 코드는 **클래스 내부**에 작성
* `main()`은 프로그램 시작점
* **컴파일 과정**
  `.java` → (컴파일) → `.class`(바이트코드) → JVM 실행

---

## 2. 변수 & 자료형(Data Types)

### 📍 기본형(Primitive Types)

| 타입      | 크기   | 예시       |
| ------- | ---- | ------------ |
| byte    | 1B   | -128~127     |
| short   | 2B   | -32,768~     |
| int     | 4B   | 기본 정수형    |
| long    | 8B   | 숫자 뒤에 L   |
| float   | 4B   | 숫자 뒤에 F   |
| double  | 8B   | 기본 실수형    |
| char    | 2B   | 유니코드 문자  |
| boolean | 1bit | true / false |

### 📍 참조형(Reference Types)

* String, 배열, 클래스 등 → Heap에 저장

---

## 3. 연산자(Operators)

| 종류 | 예시              |   |    |
| -- | -------------------| - | -- |
| 산술 | `+ - * / %`       |   |    |
| 비교 | `< > <= >= == !=` |   |    |
| 논리 | `&&               |   | !` |
| 대입 | `= += -= *=`      |   |    |
| 증감 | `++ --`           |   |    |

---

## 4. 조건문

```java
if (x > 0) {
    ...
} else if (x == 0) {
    ...
} else {
    ...
}
```

```java
switch (menu) {
    case 1: break;
    default: break;
}
```

---

## 5. 반복문

```java
for (int i = 0; i < 5; i++) {}
while (조건) {}
do {} while (조건);
```

### 향상된 for문

```java
for (int num : arr) {}
```

---

## 6. 배열(Array)

```java
int[] nums = {1, 2, 3};
int[] arr = new int[5];
```

---

## 7. 메서드(Method)

```java
public static int add(int a, int b) {
    return a + b;
}
```

> 매개변수(Parameter) vs 인자(Argument) 구분 중요

---

## 8. 클래스 & 객체 지향 기초(OOP)

### 📍 핵심 4원칙

| 개념  | 설명                               |
| --- | -------------------------------- |
| 캡슐화 | 데이터 보호 (private + getter/setter) |
| 상속  | 코드 재사용 (extends)                 |
| 다형성 | 동일 메서드 → 다른 동작 (오버라이딩/오버로딩)      |
| 추상화 | 핵심 정보만 보이기                       |

---

### 생성자(Constructor)

```java
public class Car {
    String model;
    Car(String model) {
        this.model = model;
    }
}
```

---

## 9. 접근 제어자(Access Modifiers)

| 제어자       | 접근 범위       |
| --------- | ----------- |
| public    | 전체          |
| protected | 같은 패키지 + 상속 |
| default   | 같은 패키지      |
| private   | 같은 클래스      |

---

## 10. 예외 처리(Exception Handling)

```java
try {

} catch(Exception e) {

} finally {

}
```

예외 종류

* Checked Exception (컴파일 시 체크)
* Unchecked Exception (Runtime Exception)

---

## 11. 주요 자바 API

| 클래스       | 역할     |
| --------- | ------ |
| String    | 문자열 처리 |
| ArrayList | 동적 배열  |
| HashMap   | 키-값 저장 |
| Scanner   | 입력 처리  |
| Math      | 수학 계산  |

---

## 12. 메모리 구조

| 영역          | 역할           |
| ----------- | ------------ |
| Stack       | 지역 변수, 호출 정보 |
| Heap        | 객체           |
| Method Area | 클래스 정보       |

---

## 13. Java 실행 특징

✔ 플랫폼 독립적 — JVM 위에서 실행
✔ GC(가비지 컬렉션) 자동 메모리 관리
✔ 객체 지향 언어

---

# ✨ 요약

| 주제   | 포인트                   |
| ---- | --------------------- |
| 문법   | 조건문, 반복문 확실히          |
| 객체지향 | 4대 원칙 암기              |
| 자료구조 | Array & Collection 기본 |
| 예외처리 | try-catch 사용 익히기      |

---

